services:
  - type: web
    name: aivora
    runtime: python
    plan: free
    buildCommand: "python -m compileall app.py"
    startCommand: "python app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
